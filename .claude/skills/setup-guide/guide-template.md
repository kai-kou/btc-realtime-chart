# {SERVICE_NAME} のセットアップ（ユーザー操作ぶん）

> このファイルは `{PROJECT_NAME}` を動かすために **あなたが画面で行う必要がある操作** だけを並べたものである。
> Claude が CLI・API で代行できる設定は既に済んでいるか、依頼すればこのセッションで実行できる。
> ここに残っているのは **あなたのアカウント権限が物理的に要るものだけ** である。
>
> 例示値はすべて架空値（`example.com` 等）である。実際の値に読み替えて入力する。
> API キー・トークンなどの秘密情報は、発行された画面から直接貼り付ける（チャットや Issue に書かない）。

## 全体像（所要 {MINUTES} 分）

| # | 工程 | どこで | 完了条件（自分で確認できる形） |
|---|------|--------|------------------------------|
| 1 | {工程名} | {画面名} | {観測できる状態} |
| 2 | {工程名} | {画面名} | {観測できる状態} |

**事前に必要なもの**: {アカウント / 権限 / 支払い手段 など。無ければ「特になし」}

---

## 1. {工程名}

{この工程が必要な理由を 1 行。自動化できない理由（例: 認可画面での本人の同意が要る）}

1. [{公式ページ名}]({WebFetch で確認した公式 URL}) を開く
2. {クリックする箇所を 1 動作}
3. {入力する値があれば、1 項目 1 コードブロックで下に続ける}

**{入力欄のラベル}** に入力:

```text
https://app.example.com/callback
```

**完了条件**: {ユーザーが画面で自分で確かめられる観測を 1 つ。例: 一覧に 1 行増えている}

---

## 2. {工程名}

1. {操作}
2. {操作}

**完了条件**: {観測できる状態}

---

## 付録: あとから変えたくなったら

| 変えたいもの | 経路 |
|---|---|
| {設定項目} | {画面 / Claude に依頼} |
